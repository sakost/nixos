{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$CLAUDE_FILE_PATHS\" | while read -r f; do if [[ \"$f\" == *.nix ]]; then nix-instantiate --parse \"$f\" > /dev/null 2>&1 || echo \"WARNING: Nix syntax error in $f\"; fi; done"
          }
        ]
      }
    ]
  }
}
