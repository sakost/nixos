# Tools

Enabled bundled plugins:

- `summarize` — summarize URLs, PDFs, YouTube videos.

Toggle others in `home/programs/openclaw/default.nix` under
`programs.openclaw.bundledPlugins`. See
`https://github.com/openclaw/nix-openclaw` for the full catalog.
